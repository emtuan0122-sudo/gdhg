name: Ubuntu VPS #09
on: { workflow_dispatch: {} }

jobs:
  vps09:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Start SSH session (tmate) - #09
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
