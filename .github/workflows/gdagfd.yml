name: Ubuntu VPS #03
on: { workflow_dispatch: {} }

jobs:
  vps03:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Start SSH session (tmate) - #03
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: false
